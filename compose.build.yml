services:
  rest_api_name:
    build:
      context: .
      dockerfile: dockerfile.prod
    image: drglasdou/rest_api:${APP_VERSION:-1.0.0}

  rest_api_latest:
    build:
      context: .
      dockerfile: dockerfile.prod
    image: drglasdou/rest_api:latest
